
function rays = generate_1d_rays(plane, point1, point2, n)


rays = zeros(4, 2*n);
if(plane == 'x')
    rays(1, 1:n) = point1;
    rays(1, n+1:end) = point2;
    rays(2, 1:n) = linspace(-pi/20,pi/20,n);
    rays(2, n+1:end) = linspace(-pi/20,pi/20,n);
end



end